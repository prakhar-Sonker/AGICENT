//we can make multiple fxn and export them to another file using export key word

export function addd(a: number, b:number): number{
  console.log(a+b);
  return a+b;
}

export function multiplyy(a: number, b:number): number{
  console.log(a*b);
  return a*b;
}